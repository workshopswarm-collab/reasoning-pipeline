- Reviewed instructkr/claw-code and concluded the main transplant value for VM’s quant-research swarm is explicit runtime stages, role/write registries, audit layers, and separation of runtime chat from committed artifacts; not the repo’s placeholder implementation.
- Reviewed local swarm pipeline and concluded current architecture already has strong research-vs-canon separation and late synthesis, but true diversity-of-opinion would require stronger blind first-pass / anti-cross-contamination enforcement if pursued later.
- Audited quant-db schema and confirmed a future stage-model could complement existing Postgres control-plane usage if execution state and epistemic state remain separate; warned against overloading existing status enums or overwriting first-pass predictions.
- VM decided to keep the current system architecture as-is for now rather than adding the proposed workflow-state schema.
- Prepared and launched a real fresh-topic swarm research run for case `case-20260401-8822f59b` (MrBeast 474M subscribers by March 31, 2026). Topic bootstrap succeeded, but internal handoff acknowledgments were unreliable.
- Diagnosed launch/runtime issue: visible Telegram start posts were not automatically executed from the launch path; internal session delivery could occur even when `sessions_send` appeared to time out; DB state could lag reality.
- Confirmed live DB schema mismatch: enum `processing_status` in the live database is missing `needs_intervention`, even though workspace migration `quant-db/migrations/004_market_pipeline_needs_intervention.sql` adds it. This breaks parts of auto-finalization/intervention handling.
- Confirmed intended role of `needs_intervention`: it is a market-level terminal/intervention state, while active idle-lane prodding is handled separately by `roles/orchestrator/pipeline-launch-procedure/runtime/scripts/watchdog_telegram_swarm_runs.py`.
- Applied code changes so visible Telegram lifecycle messages are state-linked in `update_research_run.py`: `queued -> running` now auto-posts STARTING markers, and `-> completed` auto-posts FINISHED markers when delivery metadata exists; posts are idempotent via notes fields.
- Added a new canonical runtime wrapper `roles/orchestrator/pipeline-launch-procedure/runtime/scripts/launch_dispatch_with_stateful_posts.py` intended to bootstrap topics, deliver handoffs, immediately patch successful deliveries to `running`, and rely on lifecycle-linked visible posts. Also updated runtime docs/bridge docs to make immediate post-delivery patching the canonical flow instead of manual replay.
- Important follow-up discovery: the local OpenClaw CLI surface does not expose an obvious validated `sessions_send` shell command, so the new launcher’s internal-handoff bridge is not yet production-ready. Updated the launcher to fail closed and require an explicit adapter template rather than guessing unsupported CLI behavior.
- Further investigation of installed OpenClaw code suggests the correct long-term fix is a programmatic Gateway/client helper for internal session delivery (same family as `client.request(...)` used by TUI), not manual CLI input from VM.
