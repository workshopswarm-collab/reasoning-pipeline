- Reviewed and tightened the research pipeline's QMD behavior: replaced verbose researcher-facing QMD prose with a minimal path-only block in `build_researcher_prompt.py`, while keeping `why_retrieved`, scores, and policy metadata in the planner-generated bundle/runtime manifests.
- Patched researcher/orchestrator policy so QMD retrieval minimums mean search effort/coverage rather than guaranteed hit count; explicitly forbade fabricating markets/analogs/retrieval artifacts and required honest sparse-retrieval reporting.
- Made unresolved canonical entity/driver detection an explicit completion-check obligation in researcher prompts and protocol: if a causally/structurally important item lacks a clean canonical slug, researchers must use `proposed_entities` / `proposed_drivers` instead of forcing weak fits.
- Audited `qualitative-db/20-entities` linkage hygiene: found 7 missing `related_entities` targets, 81 missing `related_drivers` targets, ~56 empty `related_entities`, and many reciprocity gaps; the largest issue was non-canonical driver labels being used in entity notes.
- Classified missing driver labels into likely ontology gaps vs domain/theme misuse vs trait/attribute misuse; notable recurring likely driver gaps included `scandals`, `alliances`, `exchange-access`, `opposition-coordination`, `anti-incumbency`, and `rule-of-law`.
- Applied one safe hygiene fix in `qualitative-db/20-entities/organizations/formula-1.md`: normalized `regulations` -> `regulation` in `related_drivers`.
- Code-grounded QMD finding: current implementation in `generate_qmd_bundle.py` is planner-gated and uses alias/title/description matching for entities, alias/focus/source-truth matching for drivers, and heuristic similar-case retrieval; it does not currently traverse `related_entities` / `related_drivers`, use a graph backend, or consume causal-graph structure.
- Live pipeline launch debug: the canonical headless launcher initially failed because `PREDQUANT_ORCHESTRATOR_URL` was not exported in this shell session, not because of a code regression. Verified that workspace `.env` / `.env.postgres.local` contain the DB URL and reran the launcher after sourcing `.env`.
