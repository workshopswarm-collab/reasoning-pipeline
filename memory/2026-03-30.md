
## Durable memory flush — pipeline launch procedure and swarm launch test
- Renamed `roles/orchestrator/research-pipeline/` to `roles/orchestrator/pipeline-launch-procedure/` and added a top-level `README.md` documenting the launch-control-plane process, runtime split, script roles, partial-failure behavior, idempotency, and end-to-end Orchestrator flow.
- Added/iterated runtime launch tooling under `roles/orchestrator/pipeline-launch-procedure/initialize/scripts/`:
  - `runtime_execute_dispatch.py` for manifest validation, launch-plan preparation, post-spawn patch building, and launch-summary finalization.
  - `run_dispatch_runtime.py` as the realtime runtime orchestration wrapper that emits the OpenClaw tool loop and reconciles spawn results into DB patch steps and a final summary.
- Tightened `dispatch_case_research.py` to emit a fuller canonical dispatch manifest with `dispatch_id`, `planner`, `case`, `market`, `runtime_defaults`, per-run `spawn_payload`, and `post_spawn_update_template`.
- Runtime/launch debugging lessons:
  - `streamTo` must not be used for `runtime="subagent"`; it is ACP-only.
  - Explicit model override `codex` failed because it resolved to disallowed `openai-codex/codex`.
  - Explicit model override `openai-codex/gpt-5.4` was accepted by `sessions_spawn` for subagents in this environment.
- DB/env lesson:
  - hardcoded DB credentials were removed from tracked scripts; orchestrator pipeline now expects `PREDQUANT_ORCHESTRATOR_URL` via local env/untracked `.env`.
  - ingest-role URL is not sufficient for orchestrator scripts; they require the orchestrator DB role and, depending on host policy, may also need proper SSL/auth settings.
- Live pipeline test succeeded through market selection, case opening, dispatch manifest generation, and runtime-prep for market `Will "Project Hail Mary" 2nd Weekend Box Office be greater than 54m?` with case id `dd0143c8-0ec8-46f3-8cbe-791e2f7c517e` / case key `case-20260330-0a94627e`.
- Successfully launched a 5-agent swarm with explicit `model=openai-codex/gpt-5.4` and `thinking=medium`. Child sessions launched:
  - base-rate: `agent:main:subagent:40fdd7c5-b8ad-48bc-9df0-0ad4779e2c32`
  - market-implied: `agent:main:subagent:78a96b33-8bdc-448d-9677-177652ae2020`
  - variant-view: `agent:main:subagent:48c46a78-ad44-402d-ae45-24bf9f09eda1`
  - risk-manager: `agent:main:subagent:66c3114d-490f-4fd5-ad2a-d83a2f0b4211`
  - catalyst-hunter: `agent:main:subagent:9cafedcb-84d9-4bbc-906a-85ef16bc9537`
- Post-launch status: user later reported `run:error terminated`; likely indicates at least one child subagent started but exited abnormally during execution. Next recommended step is to inspect the affected child session histories/error payloads to identify the exact execution failure mode.
- Git history from this session includes pushes for:
  - `335bce0` — restructure research memory and pipeline scaffolding
  - `5a924a4` — audit docs and clean repo hygiene
  - `f5c1d41` — refine pipeline launch procedure and runtime harness
  - `94a26fe` — tighten pipeline launch documentation wording
- Discord runtime-controller architecture was clarified and encoded as the intended launch substrate: researcher environments should be Discord thread-bound sessions, `research_runs` should flip to `running` only after those thread-backed sessions exist, and completion should reconcile back to `completed` / `failed` via `child_session_key`.
- OpenClaw config was updated with `tools.sessions.visibility = "agent"`, which enabled TUI -> Discord session handoff for the same agent and made headless dispatch from TUI viable.
- Added `roles/orchestrator/pipeline-launch-procedure/initialize/scripts/prepare_headless_discord_dispatch.py` as the canonical wrapper for headless TUI -> Discord dispatch preparation. It can reuse an existing manifest or do select-next-market -> open-case -> dispatch-case-research, persists the manifest into `roles/orchestrator/pipeline-launch-procedure/runtime/dispatch-manifests/`, validates the runtime plan, resolves the target Discord controller session, and emits a ready-to-send `sessions_send` bundle.
- Audited the full `pipeline-launch-procedure` folder for doc/script consistency. Concrete fixes made:
  - removed stale `.DS_Store` files and dropped the outdated "Option 3" phrasing from the runtime-controller spec
  - corrected `research_runs.started_at` semantics so queued run creation leaves it unset and the launch-time DB patch now sets it when the runtime actually transitions the run to `running`
  - updated runtime helpers so post-spawn patch payloads carry `mark_started=true` and generated `update_research_run.py` commands include `--mark-started`
  - confirmed the headless wrapper now always copies/re-emits manifests into the stable workspace manifest directory even when starting from an external `/tmp/...` manifest
  - syntax-checked all scripts and validated the patch-generation path with a simulated launched spawn result
- Discord channel configuration was brought into a working state for runtime-control use:
  - guild `1488206182723227658` (`Swarm`)
  - channel `1488206183193116805` (`active-research`)
  - allowlisted user `1488203083384225935`
  - `requireMention=false`
  - `channels.discord.threadBindings.enabled=true`
  - `channels.discord.threadBindings.spawnSubagentSessions=true`
- Verified real TUI -> Discord handoff after widening session visibility. `sessions_send` to `agent:main:discord:channel:1488206183193116805` returned `HANDOFF_OK` and confirmed thread-bound researcher spawning should be available from that Discord channel context.
- Added manifest-retention hygiene under `roles/orchestrator/pipeline-launch-procedure/runtime/dispatch-manifests/`:
  - active manifests live in `runtime/dispatch-manifests/`
  - older than 7 days should move to `runtime/dispatch-manifests/archive/YYYY-MM/`
  - no automatic deletion
  - helper script: `runtime/scripts/archive_dispatch_manifests.py` (default dry-run, use `--apply` to move files)
- Implemented the planner/runtime folder split for `roles/orchestrator/pipeline-launch-procedure/`:
  - planner lane: `planner/prompts/` and `planner/scripts/`
  - runtime lane: `runtime/scripts/` and `runtime/dispatch-manifests/`
  - moved all scripts/prompts accordingly, added `planner/README.md` and `runtime/README.md`, updated doc references, refreshed the active dispatch manifest path references, and patched cross-directory script paths so planner helpers now call runtime helpers through the new layout.
- Post-refactor validation passed:
  - all moved scripts compile
  - all moved scripts respond to `--help`
  - `runtime/scripts/prepare_headless_discord_dispatch.py` still emits a valid handoff bundle with the stable manifest path and the Discord session target `agent:main:discord:channel:1488206183193116805`
- Implemented the low-overhead on-demand Discord-bound dispatcher scaffolding:
  - new helper `runtime/scripts/list_pending_dispatch_manifests.py`
  - new design doc `DISCORD_ON_DEMAND_DISPATCHER.md`
  - new disabled cron job `discord-swarm-dispatcher` with job id `61a21e31-0511-4bb3-92a9-b6b50287689f`, bound to `session:agent:main:discord:channel:1488206183193116805`
  - runtime DB helper scripts now auto-load the workspace `.env` if `PREDQUANT_ORCHESTRATOR_URL` is missing from the shell environment
- Live cron-dispatch test result: the bound cron turn executed in the Discord channel session and got past manifest discovery/preparation, but the first researcher launch still failed with `Unable to create or bind a thread for this subagent session. Session mode is unavailable for this target.` So the dispatcher plumbing exists, but the underlying Discord thread-bound spawn limitation still blocks the current architecture.
- Confirmed architecture constraint from OpenClaw community answer: Discord thread-bound subagent spawn is supported, but the spawn must originate from a true Discord thread-capable target; CLI TUI is not such a target. That pushed the design away from auto-created per-run threads and toward fixed Discord persona channels.
- Reworked `roles/orchestrator/pipeline-launch-procedure/` around the fixed-channel model:
  - added `runtime/persona-channel-map.json` with controller + persona lane mappings
  - planner manifests now emit per-persona `handoff` payloads for `sessions_send` instead of thread-bound `spawn` payloads for `sessions_spawn`
  - runtime helpers now prepare/apply fixed-channel handoff delivery and post-handoff DB patches (`delivery_target_session_key`, `delivery_target_channel_id`, `persona_channel_running`)
  - added `runtime/scripts/load_dispatch_existing_state.py` so dispatch prep is idempotent against current `research_runs` DB state
  - rewrote core docs (`README.md`, `RESEARCH_DISPATCH_SPEC.md`, `OPENCLAW_RUNTIME_BRIDGE.md`, `RUNTIME_HARNESS_PROCEDURE.md`) to make fixed Discord persona lanes the canonical runtime surface
  - replaced old controller/thread docs (`SWARM_RUNTIME_CONTROLLER_SPEC.md`, `RUNTIME_CONTROLLER_PROMPT.md`, `DISCORD_ON_DEMAND_DISPATCHER.md`) with explicit deprecation notes
  - moved old thread-model manifests out of the active manifest queue into archive so the active runtime tree no longer contradicts the fixed-channel model
- Post-refactor smoke test passed: `runtime/scripts/prepare_headless_discord_dispatch.py` now emits `ready_for_fixed_channel_handoff` with five prepared `sessions_send` steps, one for each persona channel.
- Completed a full consistency audit of `roles/orchestrator/pipeline-launch-procedure/` after the move to fixed Discord persona channels.
  - active docs (`README.md`, `RESEARCH_DISPATCH_SPEC.md`, `OPENCLAW_RUNTIME_BRIDGE.md`, `RUNTIME_HARNESS_PROCEDURE.md`, `DISPATCH_LIVE_SWARM.md`, `planner/README.md`, `runtime/README.md`) now describe the same live architecture
  - docs now explicitly state that `sessions_send` handoff is internal session delivery, not a guaranteed visible Discord kickoff post
  - canonical visible lane-update format is documented as:
    - `STARTING RESEARCH | market=... | persona=... | research_run_id=...`
    - `FINISHED RESEARCH | market=... | persona=... | research_run_id=... | agent_finding_path=...`
  - added and documented the artifact/DB safety-net reconciler `runtime/scripts/reconcile_dispatch_from_artifacts.py`
  - active scripts still compile and the wrapper smoke test still returns `ready_for_fixed_channel_handoff` with `runtime_surface = discord-fixed-channel`
- Removed the old thread-model leftovers from the active workspace:
  - deleted the backward-compatibility `child_session_key` fallback from `runtime/scripts/reconcile_research_run_completion.py` so completion reconciliation is now `research_run_id`-only
  - moved deprecated docs (`SWARM_RUNTIME_CONTROLLER_SPEC.md`, `RUNTIME_CONTROLLER_PROMPT.md`, `DISCORD_ON_DEMAND_DISPATCHER.md`) to Trash staging instead of keeping deprecation notes in the repo
  - moved old archived test manifests out of `runtime/dispatch-manifests/` into Trash staging so the active pipeline tree no longer carries old thread-era artifacts
  - repurposed `runtime/scripts/archive_dispatch_manifests.py` into a Trash-backed manifest retirement helper so it no longer assumes an in-repo archive tree
- Researcher learning-loop preference: if a researcher thinks it has a preliminary idea worth proposing to `qualitative-db/40-research/review-queue/`, it should first search `qualitative-db/40-research/` for similar prior ideas/mechanisms/drivers/lessons and only promote the idea if it still looks sufficiently novel, recurring, or reusable after that search.
- Briefly experimented with stricter visible-start gating (`awaiting_visible_start` + `confirm_research_run_started.py`), but rolled it back the same day because the persona channels still did not reliably emit visible `STARTING RESEARCH | ...` markers. Current operating model is back to simpler internal-start truth: successful internal handoff can move a run to `running`, while visible lane messages remain a desired-but-not-enforced prompt behavior.
- Ran the first live fixed-channel dispatch through the new loop successfully for case `932c9965-9186-446e-a994-b18a24923945` / `case-20260330-66b100b7` on market `US x Iran meeting by June 30, 2026?` (dispatch `dispatch-case-20260330-66b100b7-20260330T195159Z`).
  - Delivered five persona assignments into the mapped Discord channels via `sessions_send`
  - Replayed the handoff results through `runtime/scripts/run_dispatch_runtime.py --handoff-results-json ...`
  - Applied five DB patch steps via `runtime/scripts/update_research_run.py`
  - All five `research_runs` rows flipped from `queued` to `running` with `started_at` populated and `dispatch_stage=persona_channel_running`
  - Notes now store `delivery_target_session_key` / `delivery_target_channel_id` instead of thread/subagent child-session metadata

## Durable memory flush — fixed Discord persona-lane refactor and live gas-case lessons
- Fixed Discord persona channels are now the canonical research runtime surface for the orchestrator swarm, replacing the attempted thread-bound Discord subagent model that proved non-viable from TUI/CLI dispatch flows.
- Active fixed-channel mapping for the swarm:
  - controller `#active-research` -> `1488206183193116805`
  - `#research-base-rate` -> `1488258861520650373`
  - `#research-market-implied` -> `1488258897117581473`
  - `#research-variant-view` -> `1488258922702962962`
  - `#research-risk-manager` -> `1488258953275117640`
  - `#research-catalyst-hunter` -> `1488258980592746586`
- Canonical routing map file for the fixed-channel architecture: `roles/orchestrator/pipeline-launch-procedure/runtime/persona-channel-map.json`.
- The pipeline-launch procedure was fully reorganized into planner/runtime lanes around this fixed-channel model. Key runtime helpers now include `load_dispatch_existing_state.py`, `reconcile_dispatch_from_artifacts.py`, and `finalize_dispatch_after_swarm.py`; the standard end-of-run repair step is `runtime/scripts/finalize_dispatch_after_swarm.py --file <manifest> --apply`.
- Runtime DB helpers now auto-load the workspace `.env`, and research-run completion reconciliation is intentionally `research_run_id`-only; the old `child_session_key` backward-compatibility path was removed from the active workspace.
- Researcher prompts/contracts were updated so drivers/mechanisms are surfaced when genuinely relevant rather than forced uniformly, and any proposed promotion to `qualitative-db/40-research/review-queue/` should first search `qualitative-db/40-research/` to check whether the idea already recurs.
- Historical/deprecated thread-era docs and manifests were removed from the active workspace and moved to Trash staging; the active repo should not keep deprecation/bwc clutter around the current pipeline.
- The full fixed-channel refactor was pushed to GitHub on `main` as commit `9b032f9` with message `Refactor pipeline launch procedure for Discord persona lanes`.
- Reconciled stale older case state mismatches so `case-20260330-66b100b7` (`US x Iran meeting by June 30, 2026?`) and `case-20260330-a326a053` (`Will Russia capture all of Huliaipole by April 30`) are intended fully complete across all five runs.
- Important live-ops lesson from gas case `case-20260330-3e291fe4` (`Will gas hit (High) $4.00 by March 31?`): generic persona instructions that say "read the newest dispatch manifest" can cause dispatch cross-contamination. During the gas run, `risk-manager` and `catalyst-hunter` were diverted onto a newer Elon case, while gas-specific DB rows remained queued.
- Current gas-case truth at last check: `base-rate` and `market-implied` completed; `variant-view`, `risk-manager`, and `catalyst-hunter` remained queued. This is a mixed-state routing problem rather than a pure full-swarm hang.
- Operational rule reinforced: do not refactor pipeline behavior mid-swarm if avoidable; apply prompt/runtime changes before launching a new swarm, and use explicit case/run-specific nudges rather than generic newest-manifest instructions when recovering stalled fixed-channel lanes.
